const Hi = () => "Hello world!";
const double = (x) => x*2;
const add = (a,b) =>  a+b;
const person = {
    name: "Alice",
    sayHi: () => "Hi, " + this.name + "!";
}

const numbers = [1,2,3,4,5];

const doubled = [];
numbers.forEach(num = > doubled.push(num * 2));

